{
  "name": "cognexus-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/minimatch": "*",
    "turbo": "^1.10.0"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "packageManager": "pnpm@10.15.0"
}
